using System;
using System.Globalization;
class armstrong
{
    static void Main()
    {
        int sum = 0, temp = 0;
        while (temp > 0)
        {
            int digit = temp % 10;
            sum += digit * digit * digit;
            temp /= 10;
        }
        if (num == sum)
        {
            Console.WriteLine("armstrong");
        }
    }
}
